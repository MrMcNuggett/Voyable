Single-line text field used across forms (advisory request, trip name, search).
```jsx
<Input label="Destination" placeholder="e.g. Hanoi" icon={<MapPinIcon/>} />
```
Shows a soft petrol focus ring; error prop swaps border/label to state-error and prints a message below.
