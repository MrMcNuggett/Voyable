Inline range calendar for picking a trip's start and end date — click once for start, again for end.
```jsx
<DatePicker onChange={({start,end})=>{}} />
```
Selected range fills with a soft petrol tint; the two endpoint days get a solid filled pill.
