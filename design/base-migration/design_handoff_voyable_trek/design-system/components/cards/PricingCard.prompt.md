Pricing tier card for Voyable's revenue streams: the always-free planner, the optional AI planning subscription, and personal travel advisory (priced per session).
```jsx
<PricingCard tier="AI Copilot" price="€9" period="/mo" description="Smart routing & suggestions" features={["AI itinerary drafts","Live re-routing","Priority support"]} featured />
```
Set `featured` on exactly one card per row (fills petrol) to anchor the recommended tier. The base trip planner has no paywall — there is no "free" pricing card, just the always-available product.
